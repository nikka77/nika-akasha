# Leçons — nika-akasha

| Date | Ce qui s'est mal passé | Règle à suivre la prochaine fois |
|---|---|---|
| 2026-08-23 | Le contre-vérificateur de la vague B a déclaré la parité FAUSSE (cœur 3 sections, zone 8 sur `baby`) en comptant les titres dans le HTML brut : React 19 streame les grands textes hors ligne (`<template id="P:n">` + `<div hidden id="S:n">` recollés par `$RS`), et le cœur (Next 16.2) découpe là où la zone (16.3) ne découpe pas. Dans le DOM, les deux rendent 8. | Une parité se mesure dans le DOM (navigateur) ou, sur le HTML brut, en comptant TOUTES les occurrences du motif y compris dans les morceaux `S:n` — jamais dans le seul bloc inline. Avant de crier à la divergence de données, interroger la base : ici elle avait 8 lignes, donc le « 3 » ne pouvait être qu'un artefact de mesure. |
| 2026-08-23 | `vercel env pull` a laissé un `.env.vercel-check` dans le dépôt, non couvert par `.env*.local`. Non commité, mais rien ne l'empêchait. | `.gitignore` couvre `.env*` entier (sauf `.env.example`). Tout fichier d'env temporaire se crée dans le scratchpad, jamais dans le dépôt. |
| 2026-08-23 | Toutes les fiches AKASHA étaient rendues dynamiquement malgré `revalidate = 3600` : le client Supabase hérité du cœur appelait `cookies()` (API dynamique) et `[slug]` n'avait pas de `generateStaticParams`. Résultat mesuré sur la prod du cœur : `no-store` partout, 874 K invocations/mois, cycle Hobby Vercel dépassé. | Dans une zone sans auth, client Supabase SANS cookies ; sur tout segment dynamique qu'on veut en ISR, `generateStaticParams` (même vide). Vérifier au build que la route est ● et non ƒ, puis en prod `x-vercel-cache: HIT` au second appel. |
| 2026-08-23 | Jour de la fusion : (1) `/learn/akasha/random` redirigeait vers le host de la zone (URL bâtie sur `request.url`, qui porte le host proxifié) ; (2) toutes les og:image pointaient sur `nika-murex.vercel.app/<slug>/opengraph-image` sans `/learn/akasha` → 404 sur chaque partage, depuis l'extraction. Le contre-vérificateur B2 comparait title/description/sections, pas les URL absolues. | Derrière une réécriture de zone, JAMAIS `request.url` pour bâtir une URL publique : `SITE_URL`. `metadataBase` = domaine du cœur + basePath + slash final. La parité d'une zone inclut TOUTES les URL absolues émises (canonical, og:url, og:image, Location des redirections). |
| 2026-08-23 | En prod, 182 liens du hub Naruto (chips d'axes) et 35 du registre (rails) étaient « /learn/akasha/learn/akasha/… » → 404 : 9 constructeurs d'URL avaient gardé le préfixe en dur dans des <Link>. L'exécutant B1 disait « adapté partout », le vérificateur B2 n'avait ouvert que 10 routes. | Un « partout » se prouve par grep exhaustif du littéral, pas par échantillon de routes. Garde-fou : lib/akasha/prefixe-basepath.test.ts refuse tout `/learn/akasha` littéral hors contexte autorisé (annoté). Tester un hub ET un registre filtré, pas seulement des fiches. |
